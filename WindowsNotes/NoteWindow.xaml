<Window x:Class="WindowsNotes.NoteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Note"
        Width="365"
        Height="427"
        WindowStartupLocation="Manual"
        ResizeMode="CanResizeWithGrip"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True">

    <!-- Sticky note visual -->
    <Border CornerRadius="10"
            Background="Yellow"
            BorderBrush="Black"
            BorderThickness="1"
            Padding="8">

        <Grid>
            <!-- Layout:
                 Row 0 = header (title + buttons)
                 Row 1 = body text -->
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header row has 4 columns:
                 [TitleBox][Mini][_Restore][Close] -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <!-- title stretches -->
                <ColumnDefinition Width="Auto"/>
                <!-- minimize -->
                <ColumnDefinition Width="Auto"/>
                <!-- restore -->
                <ColumnDefinition Width="Auto"/>
                <!-- close -->
            </Grid.ColumnDefinitions>

            <!-- Title / drag area -->
            <TextBox x:Name="TitleBox"
                     Grid.Row="0"
                     Grid.Column="0"
                     Margin="0,0,8,8"
                     MinWidth="120"
                     FontWeight="Bold"
                     FontSize="16"
                     BorderThickness="0"
                     Background="Yellow"
                     TextWrapping="NoWrap"
                     VerticalAlignment="Center"/>

            <!-- Minimize button -->
            <Button x:Name="MiniButton"
                    Grid.Row="0"
                    Grid.Column="1"
                    Margin="0,0,4,8"
                    Width="28"
                    Height="24"
                    FontWeight="Bold"
                    FontSize="14"
                    Background="Transparent"
                    BorderThickness="0"
                    ToolTip="Minimize"
                    Click="MiniButton_Click">_</Button>

            <!-- Restore button (hidden until mini mode) -->
            <Button x:Name="RestoreButton"
                    Grid.Row="0"
                    Grid.Column="2"
                    Margin="0,0,4,8"
                    Width="28"
                    Height="24"
                    FontWeight="Bold"
                    FontSize="14"
                    Background="Transparent"
                    BorderThickness="0"
                    ToolTip="Restore"
                    Visibility="Collapsed"
                    Click="RestoreButton_Click">▢</Button>

            <!-- Close button -->
            <Button x:Name="CloseButton"
                    Grid.Row="0"
                    Grid.Column="3"
                    Margin="0,0,0,8"
                    Width="28"
                    Height="24"
                    FontWeight="Bold"
                    FontSize="14"
                    Background="Transparent"
                    BorderThickness="0"
                    ToolTip="Close"
                    Click="CloseButton_Click">×</Button>

            <!-- Body text area -->
            <ScrollViewer Grid.Row="1"
                          Grid.ColumnSpan="4"
                          VerticalScrollBarVisibility="Auto"
                          Background="Yellow">
                <TextBox x:Name="BodyBox"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         Background="Yellow"/>
            </ScrollViewer>

        </Grid>
    </Border>
</Window>

